{
  "settings": [
    {
      "key": "contacts",
      "label": "Contacts (JSON)",
      "type": "text",
      "description": "JSON array of contacts: [{\"id\":\"alex\",\"name\":\"Alex\",\"relationship\":\"boss\"}]",
      "default": "[]"
    },
    {
      "key": "active_archetype",
      "label": "Default Archetype",
      "type": "select",
      "description": "Choose your default archetype for training mode",
      "options": [
        { "label": "None", "value": "none" },
        { "label": "The Ambassador — Build bridges, not walls", "value": "ambassador" },
        { "label": "The Spy — Learn everything, reveal nothing", "value": "spy" },
        { "label": "The Interrogator — Hold the line, get the truth", "value": "interrogator" },
        { "label": "The Commander — Decide, act, lead", "value": "commander" },
        { "label": "The Guardian — Protect, anchor, steady", "value": "guardian" },
        { "label": "The Provocateur — Disrupt, challenge, evolve", "value": "provocateur" }
      ],
      "default": "none"
    },
    {
      "key": "whisper_sensitivity",
      "label": "Whisper Sensitivity",
      "type": "select",
      "description": "How often Mirror surfaces insights during conversation",
      "options": [
        { "label": "Minimal (1 per conversation)", "value": "minimal" },
        { "label": "Standard (2 per conversation)", "value": "standard" },
        { "label": "Training (4 per conversation)", "value": "training" }
      ],
      "default": "standard"
    },
    {
      "key": "quiet_mode_threshold",
      "label": "Quiet Mode Threshold",
      "type": "slider",
      "description": "Emotional intensity level that triggers automatic quiet mode (lower = more sensitive)",
      "min": 50,
      "max": 95,
      "step": 5,
      "default": 75
    },
    {
      "key": "ai_api_key",
      "label": "AI API Key (for replay mode)",
      "type": "text",
      "description": "Anthropic API key for debrief replay simulation. Optional — replay works without it but won't generate AI responses."
    }
  ]
}
